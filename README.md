class cajero:
  def __init__(self, saldo_inicial):
    self.saldo
if saldo_inicial > 10000:

else:
  print("el saldo debe ser mayor a $10,000")
  self.saldo = 10001

def depositar(self, monto):
  self.saldo += monto

def retirar(self, monto):
elif self.saldo - monto < 10000:

def mostrar_saldo(self):
saldo_inicial = float(input("ingresa el saldo inicial: "))

cajero1 = cajero(saldo_inicial)
